<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GranthaVikri" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="icon">@color/icon_color_dark</item>
        <item name="android:textColor">@color/white</item>

    </style>

<!--    &lt;!&ndash; res/values-night/styles.xml &ndash;&gt;-->
    <style name="Base.Theme.GranthaVikri.Toolbar" parent="Widget.AppCompat.Toolbar">
        <!-- Customize toolbar for dark theme -->
        <item name="android:textColor">@color/black</item>
        <!-- Add other toolbar customizations for dark theme -->
    </style>


</resources>
