<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GranthaVikri" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primary</item>
        <item name="icon">@color/icon_color_light</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- res/values/styles.xml -->
    <style name="Base.Theme.GranthaVikri.Toolbar" parent="Widget.AppCompat.Toolbar">
        <!-- Customize toolbar for light theme -->
        <item name="android:textColor">@color/black</item>
        <!-- Add other toolbar customizations for light theme -->
    </style>

    <style name="Theme.GranthaVikri" parent="Base.Theme.GranthaVikri" />
</resources>